# React Native Viewer

Starter macOS app project for a future React Native Hermes console viewer.

## Open the project

Open `React Native Viewer.xcodeproj` in Xcode 26+.

## Current status

- macOS SwiftUI app scaffold
- Shared scheme for `xcodebuild`
- Starter screen aligned with the planned V1 scope
- Unit test target wired up
